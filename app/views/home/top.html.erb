<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>

<body>
<div class="body">
  <div class="container">
    <div class="row">
      <h1>welcome to 
          <strong class="glyphicon glyphicon-book">Bookers</strong> !!
      </h1>
      <p>In  <span class='glyphicon glyphicon-book'></span><strong>Bookers</strong>, </p>
          <p>you can share and exchange  your opinions , inpressions , and emotions</p>
          <p> about varioius books!</p>
    </div>
    <div class="btn-wrapper">
      <div class="row">
        <% if user_signed_in? %>
        <div><%= link_to "Log in", user_path(current_user.id), class: "btn btn-info col-xs-10 col-xs-offset-1 sign_in" %></div>
        <div><%= link_to " Sign Up", user_path(current_user.id), class: "btn btn-success col-xs-10 col-xs-offset-1 sign_up" %></div>
        <% else %>
        <div><%= link_to "Log in", new_user_session_path, class: "btn btn-info col-xs-10 col-xs-offset-1 sign_in" %></li>
        <div><%= link_to "Sign Up", new_user_registration_path, class: "btn btn-success col-xs-10 col-xs-offset-1 sign_up" %></div>
        <% end %>
          </div> 
    </div>
  </div>
</div>
</body>